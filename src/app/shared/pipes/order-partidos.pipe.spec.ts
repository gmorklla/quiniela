import { OrderPartidosPipe } from './order-partidos.pipe';

describe('OrderPartidosPipe', () => {
  it('create an instance', () => {
    const pipe = new OrderPartidosPipe();
    expect(pipe).toBeTruthy();
  });
});
